import styled from "@emotion/styled";

export const S = {};

S.Container = styled.div({
  display: "flex",
  alignItems: "center",
});

S.Input = styled.input({
  width: 20,
  height: 20,
  marginRight: 10,
});
