import styled from "@emotion/styled";

export const StyledHeader = styled.header({
  height: 50,
  lineHeight: "50px",
  borderBottom: "1px solid #000",
  textAlign: "center",
  position: "relative",
});
